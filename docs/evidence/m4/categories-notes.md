# Milestone 4 Notes

- Agents now carry a category and inherit a rubric plus a default severity threshold.
- `DEFI_TRADING` is stricter by default than `OTHER`.
- The UI can register agents by category and show the rubric catalog.
