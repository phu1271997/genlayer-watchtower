# Milestone 7 Notes

- The dashboard now supports a persistent dark/light toggle.
- `?demo=1` surfaces a demo banner and a live audit feed view.
- Wallet, penalty pool, loading, toast, and live feed blocks are split into their own reusable components.
